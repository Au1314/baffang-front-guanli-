## 私人电桩待审核列表功能完善方案

### 1. 优化思路

- **适配新API响应格式**：根据提供的新API响应格式调整表格列和数据处理逻辑
- **优化表格显示**：显示更符合私人电桩审核需求的字段
- **调整筛选条件**：使其更适合私人电桩审核场景
- **完善审核详情**：显示新API提供的详细信息
- **确保API调用正确**：使用正确的参数名和响应处理

### 2. 具体实现

**文件**：`src/views/PrivateStationAudit.vue`

#### 2.1 调整筛选条件

```vue
<!-- 筛选条件 -->
<div class="filter-bar" style="margin-bottom: 16px;">
  <el-select 
    v-model="auditFilter.authStatus" 
    placeholder="请选择认证状态" 
    style="width: 150px; margin-right: 10px;"
    clearable
  >
    <el-option label="待审核" :value="0" />
    <el-option label="审核中" :value="1" />
    <el-option label="已通过" :value="2" />
    <el-option label="已拒绝" :value="3" />
  </el-select>
  <el-select 
    v-model="auditFilter.auditStatus" 
    placeholder="请选择审核状态" 
    style="width: 150px; margin-right: 10px;"
    clearable
  >
    <el-option label="待审核" :value="0" />
    <el-option label="审核中" :value="1" />
    <el-option label="已通过" :value="2" />
    <el-option label="已拒绝" :value="3" />
  </el-select>
  <el-input 
    v-model="auditFilter.stationName" 
    placeholder="请输入电桩名称" 
    style="width: 200px; margin-right: 10px;" 
    clearable
    @keyup.enter="fetchAuditList"
  />
  <el-button type="primary" @click="fetchAuditList">查询</el-button>
  <el-button @click="resetAuditFilter">重置</el-button>
</div>
```

#### 2.2 优化表格列

```vue
<!-- 审核列表 -->
<el-table :data="auditList" stripe style="width: 100%" :loading="auditLoading" @row-click="handleRowClick">
  <el-table-column prop="id" label="ID" width="80" />
  <el-table-column prop="auditNo" label="审核单号" width="180" />
  <el-table-column prop="stationName" label="电桩名称" width="180" />
  <el-table-column prop="ownerId" label="所有者ID" width="120" />
  <el-table-column prop="address" label="地址" min-width="200" />
  <el-table-column prop="defaultPrice" label="默认价格" width="120" align="center">
    <template #default="scope">
      {{ scope.row.defaultPrice }}元/度
    </template>
  </el-table-column>
  <el-table-column prop="auditStatusName" label="审核状态" width="120" align="center">
    <template #default="scope">
      <el-tag :type="getStatusType(scope.row.auditStatus)">
        {{ scope.row.auditStatusName }}
      </el-tag>
    </template>
  </el-table-column>
  <el-table-column prop="authStatusName" label="认证状态" width="120" align="center">
    <template #default="scope">
      <el-tag :type="getAuthStatusType(scope.row.authStatus)">
        {{ scope.row.authStatusName }}
      </el-tag>
    </template>
  </el-table-column>
  <el-table-column prop="createTime" label="创建时间" width="180" align="center" />
  <el-table-column label="操作" width="150" fixed="right">
    <template #default="scope">
      <div class="operation-buttons">
        <!-- 查看详情 -->
        <el-button 
          size="small" 
          type="primary" 
          @click.stop="handleViewDetail(scope.row)"
          style="margin-right: 8px"
        >
          详情
        </el-button>
        
        <!-- 审核按钮（只有待审核和审核中状态可以审核） -->
        <el-button 
          v-if="scope.row.auditStatus === 0 || scope.row.auditStatus === 1"
          size="small" 
          type="success" 
          @click.stop="handleAudit(scope.row)"
        >
          审核
        </el-button>
      </div>
    </template>
  </el-table-column>
</el-table>
```

#### 2.3 调整响应式数据和方法

```javascript
// 筛选条件
const auditFilter = reactive({
  authStatus: null,
  auditStatus: null,
  stationName: ''
})

// 审核状态类型
const getStatusType = (status) => {
  const typeMap = {
    0: 'info',
    1: 'warning',
    2: 'success',
    3: 'danger'
  }
  return typeMap[status] || 'info'
}

// 认证状态类型
const getAuthStatusType = (authStatus) => {
  const typeMap = {
    0: 'info',
    1: 'warning',
    2: 'success',
    3: 'danger'
  }
  return typeMap[authStatus] || 'info'
}

// 获取审核列表
const fetchAuditList = async () => {
  try {
    auditLoading.value = true
    
    const params = {
      page: auditPage.value,
      size: auditPageSize.value,
      authStatus: auditFilter.authStatus,
      auditStatus: auditFilter.auditStatus
    }
    
    // 处理电桩名称搜索
    if (auditFilter.stationName) {
      params.stationName = auditFilter.stationName
    }
    
    // 使用新API获取私人电桩待审核列表
    const response = await authApi.getPrivateStationAuditList(params)
    
    auditList.value = response.list || []
    auditTotal.value = response.total || 0
  } catch (error) {
    console.error('获取审核列表失败:', error)
    ElMessage.error('获取审核列表失败')
  } finally {
    auditLoading.value = false
  }
}

// 重置筛选条件
const resetAuditFilter = () => {
  auditFilter.authStatus = null
  auditFilter.auditStatus = null
  auditFilter.stationName = ''
  fetchAuditList()
}
```

#### 2.4 调整审核详情弹窗

```vue
<!-- 审核详情弹窗 -->
<el-dialog
  v-model="showAuditDetail"
  title="审核详情"
  width="700px"
  :close-on-click-modal="false"
  :close-on-press-escape="false"
>
  <el-card shadow="never" v-loading="detailLoading">
    <el-descriptions :column="2" border>
      <el-descriptions-item label="电桩ID">{{ currentAudit.id }}</el-descriptions-item>
      <el-descriptions-item label="审核单号">{{ currentAudit.auditNo }}</el-descriptions-item>
      <el-descriptions-item label="电桩名称">{{ currentAudit.stationName }}</el-descriptions-item>
      <el-descriptions-item label="所有者ID">{{ currentAudit.ownerId }}</el-descriptions-item>
      <el-descriptions-item label="所有者姓名">{{ currentAudit.ownerName || '未知' }}</el-descriptions-item>
      <el-descriptions-item label="所有者电话">{{ currentAudit.ownerPhone || '未知' }}</el-descriptions-item>
      <el-descriptions-item label="地址" :span="2">{{ currentAudit.address }}</el-descriptions-item>
      <el-descriptions-item label="默认价格">{{ currentAudit.defaultPrice }}元/度</el-descriptions-item>
      <el-descriptions-item label="审核状态">
        <el-tag :type="getStatusType(currentAudit.auditStatus)">
          {{ currentAudit.auditStatusName }}
        </el-tag>
      </el-descriptions-item>
      <el-descriptions-item label="认证状态">
        <el-tag :type="getAuthStatusType(currentAudit.authStatus)">
          {{ currentAudit.authStatusName }}
        </el-tag>
      </el-descriptions-item>
      <el-descriptions-item label="电桩状态">
        <el-tag :type="currentAudit.status === 0 ? 'info' : 'success'">{{ currentAudit.statusName }}</el-tag>
      </el-descriptions-item>
      <el-descriptions-item label="创建时间">{{ currentAudit.createTime }}</el-descriptions-item>
      <el-descriptions-item label="充电枪数量">{{ currentAudit.chargerCount || '未知' }}</el-descriptions-item>
      <el-descriptions-item label="是否公开">{{ currentAudit.isPublic ? '是' : '否' }}</el-descriptions-item>
    </el-descriptions>
  </el-card>
  <template #footer>
    <span class="dialog-footer">
      <el-button @click="showAuditDetail = false">关闭</el-button>
      <el-button 
        v-if="currentAudit.auditStatus === 0 || currentAudit.auditStatus === 1"
        type="primary" 
        @click="handleAudit(currentAudit)"
      >
        审核
      </el-button>
    </span>
  </template>
</el-dialog>
```

### 3. 预期效果

- **更符合私人电桩审核需求**：显示电桩名称、地址、价格等关键信息
- **清晰的状态显示**：使用标签区分不同的审核状态和认证状态
- **优化的筛选条件**：可以按审核状态、认证状态和电桩名称搜索
- **详细的审核详情**：显示完整的私人电桩信息
- **正确的API调用**：使用正确的参数和响应处理

### 4. 技术要点

- 适配新的API响应格式
- 使用正确的参数名调用API
- 优化表格列显示，突出关键信息
- 使用标签组件增强状态的可视化
- 确保筛选条件与API参数匹配

### 5. 优化前后对比

**优化前**：显示通用审核字段，不针对私人电桩优化
**优化后**：专门为私人电桩审核设计，显示电桩相关的关键信息，提供更精准的筛选和操作