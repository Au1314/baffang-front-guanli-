# 重写评论管理功能方案

## 1. 问题分析

根据API文档和返回响应，当前评论管理功能存在以下问题：

- **请求参数不匹配**：
  - API要求`page`和`size`参数
  - 当前代码使用`pageNum`和`pageSize`参数

- **数据处理问题**：
  - 可能存在数据结构不匹配
  - 没有正确处理API返回的嵌套数据结构

- **功能实现不完整**：
  - 缺少一些重要功能
  - 可能存在逻辑错误

## 2. 解决方案

### 2.1 更新API请求参数

- 将`pageNum`改为`page`，`pageSize`改为`size`，与API要求一致
- 保留`username`和`keyword`筛选功能

### 2.2 完善数据处理

- 确保正确处理API返回的嵌套数据结构
- 更新数据字段映射，确保展示所有重要信息

### 2.3 重写评论管理逻辑

- 重写`fetchCommentList`函数，确保API调用正确
- 更新`commentFilter`对象，调整筛选条件
- 完善表格列定义，展示完整的评论信息
- 确保分页功能正常工作

## 3. 具体实现步骤

1. **修改`fetchCommentList`函数**：更新API请求参数和数据处理
2. **更新`commentFilter`对象**：调整筛选条件
3. **优化评论筛选区域**：确保筛选条件与API一致
4. **更新表格列定义**：展示完整的评论信息
5. **调整样式**：确保列表展示美观易用
6. **测试功能完整性**：验证所有筛选条件和分页功能正常工作

## 4. 预期效果

- 评论管理页面能够成功获取并显示评论列表数据
- 支持通过关键词、用户名进行筛选
- 分页功能正常工作
- 评论列表展示完整的评论信息，包括点赞数、回复关系、帖子标题等
- 直观区分主评论和回复评论

## 5. 文件修改

- `src/views/ForumManage.vue`：重写评论管理功能

## 6. 技术要点

- 使用Element Plus的表格组件展示评论列表
- 处理API异步调用和加载状态
- 确保API请求参数与后端一致
- 优化评论关系展示，直观区分主评论和回复

通过以上修改，评论管理功能将完全符合后端API的要求，提供完整的评论管理功能。