## 问题分析
1. **现有API路径不符**：当前`sendChatMessage`方法路径为`/customer-service/chat/${chatId}/message`，但API文档要求路径为`/customer-service/conversations/{id}/messages`
2. **参数结构需要调整**：API文档要求消息发送参数包含`content`、`messageType`和`extraInfo`
3. **需要结合WebSocket**：发送消息后，需要确保WebSocket事件能够正确处理消息

## 解决方案
1. **更新API方法路径**：修改`sendChatMessage`方法路径，使其符合API文档要求
2. **调整发送消息逻辑**：更新`handleSendMessage`函数，使用正确的参数结构
3. **确保WebSocket协同工作**：优化WebSocket事件处理，确保消息正确显示

## 实现步骤
1. **修改auth.js中的sendChatMessage方法**
   - 更新API路径为`/customer-service/conversations/${conversationId}/messages`
   - 确保参数结构正确

2. **修改handleSendMessage函数**
   - 使用HTTP API替代WebSocket发送消息
   - 构建正确的消息参数结构
   - 添加完善的错误处理
   - 优化用户体验，添加发送状态管理

3. **优化WebSocket事件处理**
   - 确保NEW_MESSAGE事件处理器能够正确处理发送的消息
   - 优化消息显示，确保新消息自动滚动到底部

4. **测试和调试**
   - 确保发送消息功能正常工作
   - 测试不同类型的消息发送
   - 确保WebSocket和HTTP API协同工作

## 预期效果
- 客服人员可以通过输入框发送消息，使用HTTP API发送到服务器
- 发送成功后，消息立即显示在聊天记录中
- 其他客服人员或用户发送的消息通过WebSocket实时接收
- 整个过程流畅，用户体验良好

## 代码修改点
1. `src/api/auth.js` - 修改`sendChatMessage`方法路径和参数
2. `src/views/CustomerService.vue` - 修改`handleSendMessage`函数
3. `src/views/CustomerService.vue` - 优化WebSocket事件处理器

## 风险评估
- API路径或参数可能与实际后端不一致，需要根据实际情况调整
- WebSocket和HTTP API可能存在时序问题，需要确保消息顺序正确
- 需要测试各种边界情况，如网络错误、API返回错误等