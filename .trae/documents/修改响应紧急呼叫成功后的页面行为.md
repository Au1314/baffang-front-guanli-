1. 修改`respondEmergencyCall`函数，在响应成功后：

   * 清除`selectedEmergencyId`，解除选中状态

   * 清除`selectedEmergency`和`handlingLogs`，重置详情数据

   * 保留`fetchPendingEmergencyList()`调用，刷新待处理列表

   * 显示成功提示信息

2. 恢复按钮的条件显示逻辑，已响应的呼叫不再显示响应按钮：

   * 重新添加`v-if="selectedEmergency && selectedEmergency.status === 0"`条件

   * 确保只有待处理状态的呼叫才显示响应按钮

3. 优化用户体验：

   * 在响应成功后，焦点自动返回到待处理列表

   * 保持响应按钮的状态同步，避免重复响应

4. 验证修改效果：

   * 确保响应成功后页面正确返回到待处理列表

   * 确保已响应的呼叫不再显示响应按钮

   * 确保其他操作（标记已处理、结束处理）仍受状态检查限制

