# 完善帖子详情查询功能方案

## 1. 问题分析
根据更新后的帖子详情API响应，当前实现需要调整以确保所有新字段都能正确显示，特别是图片和最新回复中的用户信息。

## 2. 修改内容

### 2.1 检查并调整帖子详情数据处理
- **文件**：`src/views/ForumManage.vue`
- **位置**：第1138-1139行的 `handleViewPostDetail` 函数
- **确认**：当前实现直接将 `response` 赋值给 `currentPost.value`，需要确保响应拦截器已经正确处理了新的响应格式

### 2.2 完善最新回复列表的用户头像显示
- **文件**：`src/views/ForumManage.vue`
- **位置**：第650-696行的最新回复列表
- **修改**：在回复列表中添加用户头像显示，使用 `el-avatar` 组件

### 2.3 确保所有字段正确显示
- **文件**：`src/views/ForumManage.vue`
- **位置**：第585-648行的帖子详情内容
- **确认**：帖子详情中已经包含了所有新响应字段的显示

## 3. 预期效果
- 帖子详情弹窗能正确显示所有新响应字段
- 最新回复列表中显示用户头像
- 所有图片都能正确加载和显示
- 最新回复内容完整显示

## 4. 测试计划
1. 登录管理员账号
2. 进入论坛管理页面
3. 切换到帖子管理标签页
4. 点击任意帖子查看详情
5. 确认帖子详情中所有字段显示正常
6. 确认最新回复列表中用户头像显示正常
7. 确认所有图片显示正常
8. 测试不同类型的帖子，确认详情显示逻辑正确