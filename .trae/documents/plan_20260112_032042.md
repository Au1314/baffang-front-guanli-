# 实现紧急呼叫列表功能（使用真实API）

## 问题分析
1. 当前使用模拟数据（mockEmergencyRecords），而不是真实API
2. 表格列的定义与API返回的数据结构不完全匹配
3. 两个相关方法（fetchManageEmergencyList和fetchEmergencyRecords）都需要修改为使用真实API
4. 需要确保API请求参数正确传递，包括分页、筛选等参数

## 解决方案

### 1. 修改fetchManageEmergencyList方法
- 使用authApi.getEmergencyCallRecords方法获取真实数据
- 确保请求参数正确传递，包括pageNum、pageSize、keyword、status、type等
- 处理API返回数据，更新manageEmergencyList和manageTotal

### 2. 修改fetchEmergencyRecords方法
- 使用authApi.getEmergencyCallRecords方法获取真实数据
- 确保请求参数正确传递，包括pageNum、pageSize、keyword、startTime、endTime等
- 处理API返回数据，更新emergencyRecords和recordTotal

### 3. 调整表格列定义
- 确保表格列的prop属性与API返回的数据结构匹配
- 更新状态标签的显示逻辑，使用API返回的statusName
- 更新类型标签的显示逻辑，使用API返回的typeName

### 4. 优化筛选功能
- 确保筛选条件与API请求参数匹配
- 优化日期范围筛选，确保startTime和endTime正确传递

## 具体修改点

1. **修改文件**：`src/views/Emergency.vue`
2. **修改方法**：
   - `fetchManageEmergencyList`：使用真实API获取管理紧急呼叫列表
   - `fetchEmergencyRecords`：使用真实API获取紧急呼叫记录
3. **调整表格列**：
   - 更新prop属性，与API返回数据结构匹配
   - 更新状态和类型的显示逻辑
4. **优化筛选逻辑**：确保筛选条件正确传递给API

## 预期效果
- 成功使用真实API获取紧急呼叫列表数据
- 表格正确显示API返回的数据
- 分页功能正常工作
- 筛选功能正常工作
- 状态和类型标签正确显示
- 功能正常运行，显示相应内容