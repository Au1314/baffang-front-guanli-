### 实现方案

1. **检查并更新API方法**
   - 文件：`src/api/auth.js`
   - 方法：`cancelEmergencyCall`
   - 检查请求方法是否为POST，如果不是则修改
   - 确保参数处理正确

2. **在管理紧急响应表格中添加取消呼叫按钮**
   - 文件：`src/views/Emergency.vue`
   - 位置：管理紧急响应表格的操作列
   - 条件：`v-if="scope.row.status === 1"`（只有已响应状态才显示）
   - 样式：警告类型按钮，显示"取消呼叫"

3. **实现取消呼叫逻辑**
   - 函数：`cancelEmergency(callId)`
   - 功能：处理取消呼叫操作
   - 实现：
     - 弹出带输入框的对话框，让用户输入取消原因
     - 输入框验证：确保取消原因不能为空
     - 调用API取消呼叫，传递取消原因
     - 处理API响应，显示成功或错误消息
     - 刷新管理紧急响应列表，显示最新状态

4. **添加取消呼叫对话框**
   - 使用`ElMessageBox.prompt`实现带输入框的对话框
   - 输入框类型：textarea，支持多行输入
   - 输入框占位符：提供示例取消原因
   - 输入验证：确保取消原因不能为空
   - 对话框标题："取消呼叫"

5. **错误处理**
   - 完善的错误处理机制
   - 友好的错误提示
   - 确保用户体验流畅

### 技术细节

1. **API方法检查**
   - 确保`cancelEmergencyCall`方法使用POST请求
   - 确保方法能正确处理cancelReason参数

2. **按钮添加位置**
   - 在管理紧急响应表格的操作列中，与"完成呼叫"按钮并列
   - 按钮顺序：查看详情 → 完成呼叫 → 取消呼叫

3. **对话框实现**
   - 使用`ElMessageBox.prompt`实现带输入框的对话框
   - 输入框类型：textarea，支持多行输入
   - 输入框占位符："请输入取消原因，例如：用户已自行解决问题"
   - 输入验证：确保取消原因不能为空
   - 对话框标题："取消呼叫"

4. **函数实现**
   - 函数名：`cancelEmergency`
   - 参数：callId（呼叫ID）
   - 实现：调用API取消呼叫，处理响应，刷新数据

5. **错误处理**
   - 处理API返回的错误
   - 处理网络错误
   - 处理用户取消操作
   - 显示友好的错误提示

### 预期效果

1. 在管理紧急响应列表中，已响应状态（status=1）的呼叫会显示"取消呼叫"按钮
2. 点击按钮后弹出输入对话框，让用户输入取消原因
3. 输入取消原因后点击确认，系统会调用API取消呼叫
4. 操作成功后显示成功消息，并刷新表格数据
5. 已取消的呼叫不再显示"取消呼叫"按钮

该方案符合用户需求，实现了在管理紧急响应中取消呼叫功能，并能上传取消原因到后端。