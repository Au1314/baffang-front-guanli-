# 完善帖子列表查询功能方案

## 1. 问题分析
根据更新后的API响应格式，需要修改前端代码以正确处理新的响应数据结构，主要包括：
- 响应拦截器已经返回 `res.data`，但 `fetchPostList` 函数中仍在使用 `response?.data?.list` 和 `response?.data?.total`
- 帖子列表项新增了 `coverImage`、`userAvatar`、`userName`、`replyCount` 等字段

## 2. 修改内容

### 2.1 修改 `fetchPostList` 函数
- **文件**：`src/views/ForumManage.vue`
- **位置**：第1018-1019行
- **修改**：将 `response?.data?.list` 改为 `response?.list`，将 `response?.data?.total` 改为 `response?.total`

### 2.2 检查前端表格显示
- **文件**：`src/views/ForumManage.vue`
- **位置**：第89-170行的帖子列表表格
- **确认**：表格中已经包含了 `id`、`title`、`userName`、`userId`、`likeCount`、`replyCount`、`collectCount`、`createTime`、`updateTime` 等字段，与新响应格式匹配

## 3. 预期效果
- 帖子列表能够正确显示所有新增字段
- 分页功能正常工作
- 帖子筛选和排序功能正常

## 4. 测试计划
1. 登录管理员账号
2. 进入论坛管理页面
3. 切换到帖子管理标签页
4. 查看帖子列表，确认所有字段显示正常
5. 测试分页功能，确认能够正确切换页码
6. 测试筛选功能，确认能够根据关键字、作者ID等条件筛选帖子
7. 测试排序功能，确认能够根据置顶、精华等条件排序帖子