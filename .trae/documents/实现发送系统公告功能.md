# 实现发送系统公告功能

## 1. 需求分析

根据提供的API文档，需要实现发送系统公告功能，API为 `POST /notice/broadcast`，请求体包含 `title` 和 `content` 字段。

## 2. 实现步骤

### 2.1 添加API方法

在 `src/api/auth.js` 中的 `authApi` 对象中添加发送系统公告的方法：

```javascript
// 发送系统公告
broadcastNotice(data) {
  return request.post('/notice/broadcast', data)
}
```

### 2.2 添加路由配置

在 `src/router/index.js` 中添加发送系统公告的路由：

```javascript
{
  path: '/notice',
  name: 'Notice',
  component: () => import('../views/Notice.vue'),
  meta: {
    requiresAuth: true,
    roles: [0, 1] // 超级管理员和普通管理员可访问
  },
  children: [
    // 已有的通知列表路由
    {
      path: 'list',
      name: 'NoticeList',
      component: () => import('../views/NoticeList.vue'),
      meta: {
        requiresAuth: true,
        roles: [0, 1]
      }
    },
    // 新增的发送系统公告路由
    {
      path: 'broadcast',
      name: 'NoticeBroadcast',
      component: () => import('../views/NoticeBroadcast.vue'),
      meta: {
        requiresAuth: true,
        roles: [0, 1]
      }
    }
  ]
}
```

### 2.3 创建发送系统公告组件

创建 `src/views/NoticeBroadcast.vue` 组件，包含表单和发送逻辑：

```vue
<template>
  <div class="notice-broadcast">
    <el-card>
      <template #header>
        <div class="card-header">
          <span>发送系统公告</span>
        </div>
      </template>
      
      <el-form :model="form" :rules="rules" ref="formRef" label-width="100px">
        <el-form-item label="公告标题" prop="title">
          <el-input v-model="form.title" placeholder="请输入公告标题" maxlength="50" show-word-limit />
        </el-form-item>
        
        <el-form-item label="公告内容" prop="content">
          <el-input
            v-model="form.content"
            type="textarea"
            :rows="10"
            placeholder="请输入公告内容"
            maxlength="500"
            show-word-limit
          />
        </el-form-item>
        
        <el-form-item>
          <el-button type="primary" @click="handleSubmit" :loading="loading">
            <el-icon><Send /></el-icon>
            发送公告
          </el-button>
          <el-button @click="handleReset">
            <el-icon><Refresh /></el-icon>
            重置
          </el-button>
        </el-form-item>
      </el-form>
    </el-card>
  </div>
</template>

<script setup>
import { ref } from 'vue'
import { ElMessage } from 'element-plus'
import { Send, Refresh } from '@element-plus/icons-vue'
import { authApi } from '@/api/auth'

// 表单数据
const form = ref({
  title: '',
  content: ''
})

// 表单验证规则
const rules = ref({
  title: [
    { required: true, message: '请输入公告标题', trigger: 'blur' },
    { min: 1, max: 50, message: '公告标题长度在 1 到 50 个字符', trigger: 'blur' }
  ],
  content: [
    { required: true, message: '请输入公告内容', trigger: 'blur' },
    { min: 1, max: 500, message: '公告内容长度在 1 到 500 个字符', trigger: 'blur' }
  ]
})

// 表单引用
const formRef = ref(null)

// 加载状态
const loading = ref(false)

// 提交表单
const handleSubmit = async () => {
  if (!formRef.value) return
  
  try {
    await formRef.value.validate()
    loading.value = true
    
    await authApi.broadcastNotice(form.value)
    ElMessage.success('系统公告发送成功')
    
    // 重置表单
    handleReset()
  } catch (error) {
    if (error !== false) {
      ElMessage.error('系统公告发送失败')
      console.error('发送系统公告失败:', error)
    }
  } finally {
    loading.value = false
  }
}

// 重置表单
const handleReset = () => {
  if (formRef.value) {
    formRef.value.resetFields()
  }
}
</script>

<style scoped>
.notice-broadcast {
  padding: 0 20px 20px;
}

.card-header {
  font-size: 16px;
  font-weight: bold;
  color: #303133;
}
</style>
```

### 2.4 修改Notice.vue组件

在 `src/views/Notice.vue` 中：

1. 添加发送系统公告的菜单选项：

```vue
<el-menu-item index="/notice/broadcast">
  <template #title>
    <span>发送系统公告</span>
  </template>
</el-menu-item>
```

2. 在标题映射中添加新页面：

```javascript
const titleMap = {
  '/notice/list': '通知列表',
  '/notice/broadcast': '发送系统公告'
}
```

## 3. 功能说明

1. **发送系统公告**：管理员可以通过表单输入公告标题和内容，点击发送按钮将公告发送给所有用户
2. **表单验证**：对标题和内容进行必填和长度验证
3. **权限控制**：只有超级管理员和普通管理员可以访问该功能
4. **操作反馈**：发送成功或失败都会有相应的消息提示

## 4. 技术实现

- 使用Element Plus的表单组件实现UI
- 使用Vue 3的Composition API开发
- 调用后端提供的`/notice/broadcast`接口发送公告
- 遵循现有的代码风格和架构

## 5. 测试要点

1. 验证表单验证是否正常工作
2. 验证发送功能是否正常
3. 验证权限控制是否正确
4. 验证操作反馈是否及时准确
5. 验证发送的公告是否能在通知列表中显示

## 6. 预期效果

- 管理员可以在通知管理菜单中看到"发送系统公告"选项
- 点击进入后可以填写公告标题和内容
- 提交后系统会将公告发送给所有用户
- 发送成功后会显示成功消息，并重置表单
- 发送失败会显示失败消息

## 7. 依赖关系

- 依赖现有的通知管理模块
- 依赖后端提供的`/notice/broadcast`接口
- 依赖Element Plus组件库

## 8. 代码文件列表

- 修改：`src/api/auth.js` - 添加发送系统公告API方法
- 修改：`src/views/Notice.vue` - 添加菜单选项和标题映射
- 新增：`src/views/NoticeBroadcast.vue` - 发送系统公告组件
- 修改：`src/router/index.js` - 添加路由配置