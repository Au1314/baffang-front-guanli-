## 邀请码最大使用次数功能实现计划

### 1. 表单修改
- 在邀请码生成表单中添加"最大使用次数"输入框
- 使用 el-input-number 组件，限制最小值为1
- 默认值设置为1（与表结构默认值一致）

### 2. 数据结构修改
- 在 inviteForm 中添加 maxUses 字段，默认值为1
- 确保与后端接口字段名一致

### 3. API 调用修改
- 修改 handleGenerateInviteCode 函数，将 maxUses 包含在请求数据中
- 确保传递给后端的字段名正确

### 4. 邀请码列表增强
- 在邀请码表格中添加"最大使用次数"列
- 添加"已使用次数"列
- 显示使用比例（已使用/最大使用次数）
- 更新表格列的宽度和布局

### 5. 状态显示优化
- 根据已使用次数和最大使用次数，自动更新邀请码状态
- 当已使用次数达到最大使用次数时，状态显示为"已用完"

### 6. 样式调整
- 调整表格列的宽度，确保数据完整显示
- 优化使用次数的显示样式

### 7. 验证逻辑
- 添加 maxUses 的验证规则
- 确保值为正整数

### 8. 实现步骤
1. 修改 inviteForm 数据结构
2. 更新生成邀请码表单
3. 修改 API 调用
4. 更新邀请码列表表格
5. 优化状态显示
6. 调整样式
7. 测试功能