# 修改个人资料页面字段映射

## 1. 分析当前实现
- 已实现从API获取管理员个人信息功能
- profileForm中已有username和name字段
- 需要修改UI标签和字段映射，确保正确对应

## 2. 需要修改的地方
1. **UI标签**：将"网名"改为"昵称"
2. **字段映射**：确保username对应用户名，name对应昵称
3. **表单验证**：确保验证规则正确
4. **保存逻辑**：确保保存时字段映射正确

## 3. 具体实现步骤
1. 修改UI中"网名"标签为"昵称"
2. 确保profileForm中的username和name字段正确映射到API响应
3. 检查并修改表单验证规则
4. 确保保存逻辑正确处理字段映射

## 4. 预期效果
- 个人资料页面显示"昵称"标签，对应name字段
- 用户名显示正确，对应username字段
- 所有字段映射正确，API调用成功

## 5. 注意事项
- 保持与现有代码风格一致
- 确保API响应字段正确映射
- 确保UI显示与字段对应