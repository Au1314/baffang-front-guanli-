### 实现方案

1. **更新API方法**
   - 在`auth.js`中修改`completeEmergencyCall`方法，将请求方法从PUT改为POST
   - 确保方法能正确处理remark参数

2. **添加完成呼叫按钮**
   - 在管理紧急响应表格的操作列中添加"完成呼叫"按钮
   - 按钮只在呼叫状态为已响应（status=1）时显示

3. **实现完成呼叫对话框**
   - 添加一个带有输入框的对话框，用于输入备注remark
   - 对话框包含确认和取消按钮
   - 确认按钮点击后调用API完成呼叫

4. **添加完成呼叫逻辑**
   - 实现`completeEmergency`函数，处理对话框确认事件
   - 调用API完成呼叫，传递备注信息
   - 处理API响应，显示成功或错误消息
   - 刷新表格数据，显示最新状态

5. **添加状态检查**
   - 确保只有已响应状态（status=1）的呼叫才能显示完成按钮
   - 防止重复点击和无效操作

### 技术细节

1. **API方法修改**
   - 文件：`src/api/auth.js`
   - 方法：`completeEmergencyCall`
   - 修改：将PUT请求改为POST请求

2. **按钮添加位置**
   - 文件：`src/views/Emergency.vue`
   - 位置：管理紧急响应表格的操作列
   - 条件：`v-if="scope.row.status === 1"`

3. **对话框实现**
   - 使用`ElMessageBox.prompt`实现带输入框的对话框
   - 输入框类型：textarea，用于输入备注
   - 输入框验证：确保备注不能为空

4. **函数实现**
   - 函数名：`completeEmergency`
   - 参数：callId（呼叫ID）
   - 实现：调用API完成呼叫，处理响应，刷新数据

5. **错误处理**
   - 完善的错误处理机制
   - 友好的错误提示
   - 确保用户体验流畅

### 预期效果

1. 在管理紧急响应列表中，已响应状态（status=1）的呼叫会显示"完成呼叫"按钮
2. 点击按钮后弹出输入对话框，让用户输入备注
3. 输入备注后点击确认，系统会调用API完成呼叫
4. 操作成功后显示成功消息，并刷新表格数据
5. 已完成的呼叫不再显示"完成呼叫"按钮

该方案符合用户需求，实现了在管理紧急响应中完成呼叫功能，并能上传备注到后端。