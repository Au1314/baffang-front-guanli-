1. **添加启用/禁用邀请码API**：

   * 在`src/api/auth.js`中添加启用和禁用邀请码的API方法

   * 方法名：`enableInviteCode`和`disableInviteCode`

   * 对应API路径：`/auth/invite-code/{code}/enable`和`/auth/invite-code/{code}/disable`

2. **更新状态管理**：

   * 在`src/store/adminStore.js`中添加启用和禁用邀请码的状态管理方法

   * 方法名：`enableInviteCode`和`disableInviteCode`

   * 实现调用API并更新本地状态

3. **完善界面功能**：

   * 在`src/views/PlatformManage.vue`的邀请码管理表格中添加启用/禁用按钮

   * 更新操作列逻辑，根据邀请码状态显示不同按钮

   * 添加启用/禁用操作的处理函数

   * 优化状态显示，利用新增的statusName字段

4. **更新状态逻辑**：

   * 更新`getInviteCodeStatusText`和`getInviteCodeStatusType`函数，更好地处理status字段

   * 确保状态显示与实际API返回的status保持一致

5. **测试功能完整性**：

   * 确保启用/禁用功能正常工作

   * 状态显示正确

   * 按钮状态与邀请码状态匹配

