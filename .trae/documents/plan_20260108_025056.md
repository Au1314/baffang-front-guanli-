1. **问题分析**：初始的activeTab值是"member"，但在onMounted钩子中只初始化了图表，没有调用fetchMemberList()来加载管理员列表。目前只有在切换到"member"标签页时才会调用fetchMemberList()。

2. **解决方案**：

   * 在onMounted钩子中添加fetchMemberList()调用，以实现管理员列表自动加载的功能。

3. **具体修改**：

   * **修改PlatformManage.vue**：

     * 在onMounted钩子中添加await fetchMemberList()调用

4. **验证方法**：

   * 运行项目，进入平台管理页面

   * 查看管理员列表是否在页面加载时自动显示

5. **影响范围**：

   * 仅修改了PlatformManage.vue中的onMounted钩子

6. **技术实现**：

   * 在组件挂载时调用fetchMemberList()函数

   * 使用async/await处理异步API请求

