## 问题分析
1. **HTTP请求类型不符**：根据用户提供的API文档，设置/取消置顶应该使用PUT请求，但当前代码中使用的是POST请求
2. **精华帖子功能可能存在同样问题**：精华帖子功能的实现方式类似，也可能需要检查和修复
3. **请求参数名正确**：当前代码中使用的参数名`isTop`与API文档要求一致，无需修改
4. **API路径正确**：当前代码中使用的路径与API文档要求一致，无需修改

## 修复方案
1. **修改帖子置顶API请求类型**：在 `src/api/auth.js` 中，将 `setPostTop` 方法的HTTP请求类型从POST改为PUT
2. **修改精华帖子API请求类型**：同样将 `setPostEssence` 方法的HTTP请求类型从POST改为PUT，确保与置顶功能保持一致
3. **保持请求参数和路径不变**：请求参数名和API路径已经正确，无需修改
4. **测试验证**：修复后需要确保置顶和精华功能正常工作

## 修复文件
- `src/api/auth.js`：修改帖子置顶和精华帖子的API请求类型

## 修复后效果
- 帖子置顶功能使用正确的PUT请求类型，路径为 `/forum/post/{id}/top`
- 精华帖子功能使用正确的PUT请求类型，保持与置顶功能的一致性
- 功能保持不变，只是修复了HTTP请求类型不符的问题
- 符合API文档要求，确保与后端API正确对接